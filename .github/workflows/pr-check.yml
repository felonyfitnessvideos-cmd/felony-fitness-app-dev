name: 🔍 PR Quality Check

on:
  pull_request:
    branches: [main, develop]

jobs:
  pr-check:
    name: 🚀 Quick Quality Check
    runs-on: ubuntu-latest
    timeout-minutes: 15  # Fail if takes longer than 15 minutes
    
    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v4
        
      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          
      - name: ⚡ Install Dependencies
        run: npm ci
        timeout-minutes: 5
        
      - name: 🔍 Lint Check
        run: npm run lint || true  # Don't fail on lint warnings
        timeout-minutes: 2
        
      - name: 🧪 Run Tests
        run: npm run test:ci
        timeout-minutes: 5
        
      - name: 🏗️ Build Check
        run: npm run build
        timeout-minutes: 5
        
      - name: ✅ PR Ready
        run: echo "✅ PR passed all quality checks!"